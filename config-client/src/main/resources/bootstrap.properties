#Http service has the resource in the form
#where appliation is app name, profile is name of the profile and label is the git branch 
#/{application}/{profile}[/{label}]
#/{application}-{profile}.yml
#/{label}/{application}-{profile}.yml
#/{application}-{profile}.properties
#/{label}/{application}-{profile}.properties



#application name.e.g., configclient.yml, configclient-{profile}.yml, etc
spring.application.name=application
#profile name. e.g., {application}-dev.yml
spring.profiles.active=dev
#Branch/tag name
spring.cloud.config.label=master
spring.cloud.config.uri=http://localhost:8888/
management.security.enabled=false
#server.servlet.context-path=/configclient
